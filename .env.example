# Squilla Console 环境变量示例
#
# 复制为 .env 或写进 systemd unit 的 Environment= / EnvironmentFile=。
# 全部变量都有可用默认值，最小部署可以一个都不设。
# 本文件不含任何真实凭据，也不要把填好的 .env 提交进仓库。

# ---------------------------------------------------------------- 数据目录
# 凭据库、鉴权库、模型缓存的落盘位置。默认是仓库下的 data/，
# 已被 .gitignore 排除；生产建议指向仓库外的独立目录。
# SQUILLA_CONSOLE_DATA=/var/lib/squilla-console/data

# ---------------------------------------------------------------- 网关连接
# 被管理的 OpenSquilla 网关地址。
# SQUILLA_GATEWAY_WS=ws://127.0.0.1:18791/ws
# SQUILLA_GATEWAY_HTTP=http://127.0.0.1:18791
# 网关自身若开了 token 鉴权，在这里给出。
# SQUILLA_GATEWAY_TOKEN=

# ---------------------------------------------------------------- 控制台登录
# 操作员用户名。
# SQUILLA_CONSOLE_USER=operator
#
# 初始密码。不设则首启自动生成一个随机密码，写入
# $SQUILLA_CONSOLE_DATA/bootstrap-password.txt（0600），改密后自动删除。
#
# 注意：这里给的密码只作为「初始密码」，首次登录会被强制要求修改，
# 修改后凭据存进 data/auth.json（0600, PBKDF2），本变量即失效。
# 因此不需要、也不建议在 unit 里长期保留它。
# SQUILLA_CONSOLE_PASSWORD=

# ---------------------------------------------------- OpenSquilla 安装/升级
# 控制台通过 uv 管理 OpenSquilla 的安装。若 uv 与 opensquilla 不在
# PATH 上（例如都装在数据盘），显式给出路径，否则安装面板看不到它们。
# SQUILLA_UV_BIN=/opt/opensquilla/uv-bin/uv
# SQUILLA_OS_BIN=/opt/opensquilla/uv-bin/opensquilla
# SQUILLA_OS_BASE=/opt/opensquilla
#
# uv 自身的重定向变量也应一并给出（控制台会继承给子进程）：
# UV_TOOL_DIR=/opt/opensquilla/uv-tools
# UV_TOOL_BIN_DIR=/opt/opensquilla/uv-bin
# UV_CACHE_DIR=/opt/opensquilla/uv-cache
# UV_PYTHON_INSTALL_DIR=/opt/opensquilla/uv-python
#
# 安装时使用的 Python 版本与 extras。extras 默认从 uv 的安装回执里读，
# 读不到才回落到这个值 —— 升级时保持 extras 一致很重要，
# 否则 opensquilla[recommended] 会被降级成裸 opensquilla。
# SQUILLA_OS_PYTHON=3.12
# SQUILLA_OS_EXTRAS=recommended
#
# 发布源。默认从 GitHub Releases 取 wheel；PyPI 上的版本落后多个小版本，
# 走 PyPI 会导致降级，因此代码里不使用 PyPI。
# SQUILLA_OS_REPO=opensquilla/opensquilla
# SQUILLA_OS_CHANNEL=https://opensquilla-releases.oss-cn-beijing.aliyuncs.com/releases/channels/stable.json
